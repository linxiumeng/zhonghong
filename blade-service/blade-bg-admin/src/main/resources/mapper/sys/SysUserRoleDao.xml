<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="org.springblade.bgadmin.modules.sys.dao.SysUserRoleDao">

	<select id="queryRoleIdList" resultType="long">
		select role_id from sys_user_role where user_id = #{value}
	</select>

	<select id="selectByUserId" resultType="org.springblade.bgadmin.modules.sys.entity.SysRoleEntity">
		select role.* from sys_user_role user_role join sys_role role  on role.role_id = user_role.role_id  where user_id = #{value}
	</select>

	<delete id="deleteBatch">
		delete from sys_user_role where role_id in
		<foreach item="roleId" collection="array" open="(" separator="," close=")">
			#{roleId}
		</foreach>
	</delete>
</mapper>